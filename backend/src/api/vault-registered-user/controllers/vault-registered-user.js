'use strict';

/**
 *  vault-registered-user controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vault-registered-user.vault-registered-user');
